Summary of all changes made to fix Allure:
1. pom.xml (root) — added property:


<aspectj.version>1.9.22</aspectj.version>
2. application-tests/pom.xml — added dependency:


<dependency>
    <groupId>org.aspectj</groupId>
    <artifactId>aspectjweaver</artifactId>
    <version>${aspectj.version}</version>
    <scope>test</scope>
</dependency>
And updated argLine:


<argLine>-Xmx2048m -XX:+UseG1GC -javaagent:"${settings.localRepository}/org/aspectj/aspectjweaver/${aspectj.version}/aspectjweaver-${aspectj.version}.jar"</argLine>
3. application-tests/src/test/java/com/smbc/raft/tests/bdd/runner/TaggedCucumberRunner.java — added Allure plugin to @CucumberOptions:


"io.qameta.allure.cucumber7jvm.AllureCucumber7Jvm"

It's already updated — line 250 in application-tests/pom.xml inside the maven-surefire-plugin <configuration> block:


application-tests/pom.xml
└── <build>
    └── <plugins>
        └── <plugin> maven-surefire-plugin
            └── <configuration>
                └── <argLine>   ← LINE 250
 find this section in application-tests/pom.xml:


<forkCount>1</forkCount>
<reuseForks>true</reuseForks>
<argLine>-Xmx2048m -XX:+UseG1GC</argLine>   ← original
Replace the <argLine> with:


<argLine>-Xmx2048m -XX:+UseG1GC -javaagent:"${settings.localRepository}/org/aspectj/aspectjweaver/${aspectj.version}/aspectjweaver-${aspectj.version}.jar"</argLine>
